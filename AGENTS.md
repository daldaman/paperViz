# AGENTS.md — paperViz (`pvz`)

`CLAUDE.md` at this repo root is the canonical instruction file for this repository. Read and follow it in full — its guidance is agent-agnostic despite the filename. This file is deliberately a pointer, not a mirror: the previous full copy was a blind text substitution with no sync mechanism, and it drifted (projectIndex#51, resolved 2026-08-18).

At session start read, in order: `CLAUDE.md`, `STATUS.md`, `MEMORY.md`.
